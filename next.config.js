/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'plus.unsplash.com',
      'image.uniqlo.com',
      'im.uniqlo.com'
    ],
  },
}

module.exports = nextConfig 